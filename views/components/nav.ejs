<div>
<% if (authed) { %>
    <p>[navbar] "<%= username %>" id: <%= uid %></p>
    <% if (isSuper) { %>
    <a href="/pagesuper/index">super</a>
    <% } %>
    <a href="/logout">log out</a>
<% } else { %>
    <p>[navbar] not logged in.</p>
<% } %>
</div>
